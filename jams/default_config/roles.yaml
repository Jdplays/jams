roles:
  Admin:
    description: The Administrator
    pages:
      - user_management
      - events
      - schedule_planner
      - settings
      - private_access_log
  Volunteer:
    description: A Helper at the event
    pages:
      - volunteer_attendance
      - workshop_catalog
      - locations_timeslots